# Anonymous release

This bundle contains source code, tests, configurations, and public usage
documentation only. It excludes model checkpoints, generated results, caches,
internal development records, usernames, private paths, hostnames, addresses,
credentials, and keys.

Scripts use the active Python interpreter or the `PYTHON` environment variable
and do not depend on a particular server or accelerator model.
